name: http_cache_stream
description: "Simultaneously download, cache, and stream remote content. Fork with offline-first playback support for pre-cached HLS/progressive content."
version: 0.0.6+1
homepage: https://github.com/erisanolasheni/http_cache_stream_no_local_cache_probe
repository: https://github.com/erisanolasheni/http_cache_stream_no_local_cache_probe
topics:
  - cache
  - streaming
  - video
  - audio
  - http

platforms:
  android:
  ios:
  linux:
  macos:
  windows:


environment:
  sdk: ^3.5.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  crypto: ^3.0.6
  http: ^1.3.0
  path: ^1.9.0
  path_provider: ^2.1.5
  synchronized: ^3.4.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
